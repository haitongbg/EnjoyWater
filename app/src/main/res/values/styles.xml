<resources>

    <!-- Base application theme. -->
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorControlNormal">@color/white</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.NoActionBar.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.NoActionBar.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="CustomDialogRightInAnimation">
        <item name="android:windowEnterAnimation">@anim/slide_right_to_left_in</item>
        <item name="android:windowExitAnimation">@anim/slide_left_to_right_out</item>
    </style>

    <style name="CustomDialogLeftInAnimation">
        <item name="android:windowEnterAnimation">@anim/slide_left_to_right_in</item>
        <item name="android:windowExitAnimation">@anim/slide_right_to_left_out</item>
    </style>

</resources>
